+++
id = "ISS-0011"
kind = "contract"
risk = "high"
complexity = "s"
depends_on = ["ISS-0001"]
db = false
ui = false
verify = ["pnpm run typecheck", "pnpm test", "pnpm run build"]
acceptance = [
  "resolveAttribution given a cwd inside a linked git worktree returns the main repo root as the repo root and the worktree checkout path as the worktree path; given a cwd inside a main checkout it returns that root with an absent worktree path; given a non-git cwd it returns the supplied init root fallback with an absent worktree path.",
  "resolveAttribution classifies a main checkout correctly when the git dir is not at <repo_root>/.git: given a submodule checkout, and given a git init --separate-git-dir checkout, it returns that checkout as the repo root with an absent worktree path, and never returns a repo root inside a .git directory.",
  "resolveAttribution is stable under symlinked paths: given a main checkout reached by a symlinked cwd it returns an absent worktree path and the same repo root string that the realpathed cwd yields - one repo never resolves to two repo_root identities.",
  "resolveAttribution ignores the ambient git environment: with GIT_DIR, GIT_WORK_TREE, or GIT_COMMON_DIR set in the environment to another repository, resolution still reflects the cwd's own repository, so a session's spool can never be redirected into an unrelated repo.",
  "Every resolution case holds when the cwd is a SUBDIRECTORY of the checkout, not only its root - which is how the hook artifact always calls it, since a session's cwd is where the user is working.",
  "A linked worktree whose main repo is a submodule, a --separate-git-dir checkout, or a bare repo resolves to the correct main repo root: never a path inside a .git directory, never the bare git directory itself, and never the worktree standing in for its own main root.",
  "A resolution failure returns the supplied init-root fallback, never a fabricated or synthesized path.",
]

[files]
owns = [
  "src/core/attribution.ts",
  "tests/unit/core/attribution.test.ts",
]
touches = []
+++

# Core contracts III: attribution

Split out of ISS-0001 on 2026-07-14 after it escalated twice. Attribution is
one small pure function — and it killed both ladders, because git has more ways
to lay out a repository than anyone remembers. It gets its own issue and its own
focused edge-case suite.

Prior art: `escalated/ISS-0001-attempt-2` has an implementation that handles
the worktree, main-checkout, non-git and symlink cases. It still fails the
submodule / `--separate-git-dir` case and the `GIT_DIR` leakage case. Start from
it.

## Why this function is load-bearing

`resolveAttribution` decides **which repo's spool a session's events are
appended to**. It is called by the hook artifact at capture time (to find the
spool) and by ingest (to fill `sessions.repo_root` / `worktree_path`). Getting
it wrong does not produce a bad display — it writes **ground truth into the
wrong place**, or into a place git will overwrite. There is no recovering from
that after the fact.

It must be expressible with **no package dependency**: pure `git rev-parse`
shell-outs plus path logic, because the zero-dependency hook artifact calls the
same logic.

## The contract

Given a cwd and an init-root fallback:

- cwd inside a linked **worktree** → repo root is the **main** repo root; the
  worktree checkout path is recorded alongside it.
- cwd inside a **main checkout** → that root; worktree path ABSENT.
- **non-git** cwd → the supplied init-root fallback; worktree path ABSENT.

## The mechanism (do not re-derive — three reviews established this)

**Compare `git rev-parse --git-dir` with `--git-common-dir`.**

- **Equal → a main checkout.** repo_root = `--show-toplevel`, worktree path
  ABSENT — *no matter where the git dir physically lives*.
- **Different → a genuine linked worktree.** Derive the main root from
  `git worktree list --porcelain`'s first entry.

**Never infer linkage from `dirname(gitCommonDir)`.** That assumes the git dir
is always `<repo_root>/.git`, which is false for a **submodule** (git dir lives
at `<super>/.git/modules/<name>`) and for `git init --separate-git-dir`. Attempt
1 did this, and a submodule checkout resolved to a `repo_root` **inside
`.git`** — meaning the append-only spool, the one artifact the ledger cannot
regenerate, would be written into a directory git owns and rewrites.

**Realpath both sides before comparing.** `--show-toplevel` returns a physical
path while `--git-common-dir` is relative to the caller's logical cwd. On any
symlinked path (macOS `/var` → `/private/var`, `$TMPDIR`) an unrealpathed
comparison fabricates a worktree path for a main checkout and mints **two
`repo_root` identities for one repo** — which means two registry entries and two
ledgers for the same repository.

**Neutralize the ambient git environment — ALL of it.** `GIT_DIR` /
`GIT_WORK_TREE` / **`GIT_COMMON_DIR`** in the caller's environment redirect
`git rev-parse` at another repository entirely. A session running with a stray
one would have its spool written into an unrelated repo. The first attempt
scrubbed `GIT_DIR` and `GIT_WORK_TREE` and **missed `GIT_COMMON_DIR`**
(2026-07-14 finding), which is precisely the variable this module's own
mechanism reads — so the guarantee the criterion states was false.

Scrub the whole family from the environment of every git invocation this module
makes: `GIT_DIR`, `GIT_WORK_TREE`, `GIT_COMMON_DIR`, `GIT_INDEX_FILE`,
`GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES`,
`GIT_CEILING_DIRECTORIES`. Resolution must depend on the **cwd** and nothing
else. Prefer an allowlist (build a clean env) over a denylist (delete known
names) — a denylist is one `git` release away from being wrong again.

**Resolution failure returns the init-root fallback, never a fabricated path**
(2026-07-14 finding). If git fails or returns something unparseable, fall back;
do not synthesize a plausible-looking root. A fabricated repo root writes ground
truth into the wrong place — the same class as fabricating a facet, but worse,
because it is unrecoverable.

## Invariants (contract prose, not acceptance)

- No package dependency: `git` shell-outs and path logic only.
- Pure: no writes, no network, no ledger access.
- A resolution failure returns the init-root fallback — it never throws and
  never fabricates a path.

## Test-harness contract

Unit tests only (plain vitest) at `tests/unit/core/attribution.test.ts`, against
**real git repositories** created in a tmpdir — no mocked git, ever. This
function's entire difficulty is real git's behavior; a mocked test would assert
the author's assumptions back at them, which is exactly how the ladders died.

**Every case must ALSO be exercised with a SUBDIRECTORY as the cwd** (2026-07-14
finding — the sharpest one). The first attempt only ever passed repo roots, yet
**the hook artifact always calls this from a subdirectory**: a session's cwd is
wherever the user is working, essentially never the repo root. Git's output shape
changes there (`--git-dir` and `--git-common-dir` return *relative* paths whose
resolution depends on the cwd), so the untested path is the only path that ever
runs in production. Test both roots and subdirectories, everywhere.

Build each fixture repo for real, and cross the layouts:

- `git init` main checkout; `git worktree add` linked worktree.
- A real **submodule** (`git submodule add` of a second local repo) — and a
  **linked worktree OF the submodule**, which the first attempt resolved to a
  `repo_root` **inside `.git`**: the exact unrecoverable outcome this issue
  exists to prevent.
- A **`git init --separate-git-dir`** checkout, and a **linked worktree of one**
  (resolved to the worktree itself — wrong).
- A **bare** main repo with a linked worktree (resolved to the bare git dir
  itself — wrong).
- A symlinked path to a main checkout (identity stability).
- A plain tmpdir (non-git fallback).
- A case invoked with each of `GIT_DIR`, `GIT_WORK_TREE` and **`GIT_COMMON_DIR`**
  set to a foreign repo (the ambient-env case; `GIT_COMMON_DIR` was the one that
  slipped through).

The matrix is the point: the failures all live where two layouts cross (a
worktree **of** a submodule; a worktree **of** a bare repo), and single-axis
fixtures are what let them through.

An acceptance harness exists at HEAD (`tests/acceptance/`, from ISS-0002); this
issue does not add to it. `pnpm test` must be green when you finish.
