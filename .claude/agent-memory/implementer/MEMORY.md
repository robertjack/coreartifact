- [Escape sequences in Write/Edit tool params](feedback_escape_sequences_in_tool_writes.md) — typing `\x00` in a tool call can insert a raw byte, not escape text; verify with a byte-level check, not Read.
- [Validate-before-transform is CONTEXT-DEPENDENT](feedback_validate_before_transform_spool.md) — parsed-JSON values check-raw-then-trim; raw stdin off a pipe must trim-then-check (trailing \n is legitimate framing, not corruption). Getting this backwards caused two separate data-loss bugs.
- [Sandbox has no network; @types/node is already there](reference_sandbox_no_network_types_node.md) — `pnpm add` fails and `pnpm install` can wipe node_modules; Node builtins already typecheck via the lockfile's transitive @types/node. Never hand-roll an ambient shim.
- [tests/acceptance/** writable in practice](reference_tests_acceptance_writable_in_practice.md) — the stated read-only law isn't tool-enforced here; still self-police, only edit for spec-mandated mechanical changes.
- [Totality + concurrency test patterns](feedback_totality_and_concurrency_test_patterns.md) — `typeof null === "object"` trap for JSONL folds; Promise.all over sync bodies isn't a concurrency test, spawn real processes.
- [git worktree reverse-pointer gotcha](reference_git_worktree_reverse_pointer_gotcha.md) — submodule gitdirs self-correct to their checkout via a reverse pointer file; --separate-git-dir and bare gitdirs never do, verified on git 2.55.
- [git gitdir identity discriminator](reference_git_gitdir_identity_discriminator.md) — core.worktree presence (not --is-bare-repository) is the real ALLOWED/FORBIDDEN signal for bare/separate-git-dir/submodule gitdir candidates; porcelain worktree-list mangles bare-named-.git paths, feed --git-common-dir instead.
- [Hermeticity test must isolate its own assertions too](feedback_harness_env_isolation_test_own_env_too.md) — if you mutate process.env to simulate a hostile shell, your verification execFileSync calls need the same hermetic env or the test poisons itself.
- [Rescue dispatch: escalated branch may not be on main](reference_rescue_dispatch_escalated_branch_not_on_main.md) — check `git ls-tree` on main vs the escalated branch for owned paths before assuming you're patching a diff; the whole implementation may be missing.
- [Manual CLI execution proof isolation](feedback_manual_cli_execution_proof_isolation.md) — this repo's own CLI self-propagates `init` across sibling git worktrees; Bash cwd silently defaults to main checkout, always `cd "$TMPDIR" &&` in the SAME command.
- [@ts-ignore is single-line only](feedback_ts_ignore_single_line_only.md) — reflowing a shimmed `node:fs`/`node:sqlite` import onto multiple lines moves it off the suppressed line; TS2591 comes back, surfaced confusingly via vitest globalSetup's tsc call.
- [ISS-0013 checkLine test_dispute, not scope_change](reference_iss0013_checkline_test_dispute_not_scope_change.md) — the locked acceptance test imports a module the issue's own footprint never grants; the fix is the test's import path, not a footprint grant. Don't re-request scope_change.
- [Footprint vs test module-path mismatch](reference_footprint_vs_test_module_path_mismatch.md) — ISS-0015: footprint said `operatorState.ts`, locked test hardcoded `state.js`; both sides guarded, unfixable in-session — diff import paths vs footprint before coding, report mismatch as headline finding.
- [ISS-0015 2nd test bug: os.homedir() after HOME override](reference_iss0015_second_test_bug_homedir_after_override.md) — the 4th criterion's `os.homedir()` call runs after `beforeEach` already overrode `HOME`, so it reads back the override, not the real home; the assertion fails unconditionally, independent of the module-path bug — both need fixing in one test_dispute.
- [Manifest-absent vs per-path-uncaptured](feedback_uninstall_manifest_absent_vs_per_path_uncaptured.md) — ISS-0022 F103: same empty-fold shape, opposite scope; fix with a separate whole-manifest gate at the command entry, don't retrofit the permissive per-path fold.
- [ISS-0022 F108: capture-time pollution strip](feedback_iss0022_f108_capture_time_pollution_strip.md) — a re-run recovery capture can record the tool's OWN prior output as "pre-init truth"; strip before recording, reuse existing strip logic, watch for the resulting import cycle (safe if only used inside functions).
- [Backup via shared pure-function side effect](reference_backup_via_shared_pure_function_side_effect.md) — to intercept file state before an out-of-scope command overwrites it, hang the side effect off a pure function that command already calls with the right args; guard hard against that function's own fake-path unit tests; restore verbatim, never re-serialized.
- [pnpm is broken in this sandbox](reference_pnpm_broken_in_sandbox.md) — every `pnpm` subcommand fails with "unable to open database file", environment-wide; run `node_modules/.bin/{tsc,vitest}` directly instead and report the substitution.
- [Inverting a merge needs two paths](feedback_invert_merge_two_path.md) — undoing an idempotent merge (e.g. uninstall vs init): no-backup-record means "not ours, leave alone" never "delete"; blind snapshot restore destroys edits made after the original write — compare current bytes to a recomputed "what the write produced" and branch verbatim-restore vs. surgical-strip.
- [node:sqlite concurrency has 3 layered failure modes](reference_sqlite_concurrent_first_creation_race.md) — BEGIN IMMEDIATE fixes only #1; readOnly connections need their own busy_timeout (#2); a fresh-ledger first-creation race can still delete a live writer's file with an UNSTABLE error string (#3) — test against a never-before-created ledger, not a warmed one.
- [ISS-0018 seq computed after test_results](reference_iss0018_seq_computed_after_test_results.md) — a seq-keyed mutant sees seq=0 (not a plausible off-by-a-few) since seq recompute runs after the test_results insert block; corrupt-spool-line injection is the standard way to desync line_no/seq in a unit test.
- [ISS-0018 footprint excludes cli/commands/show.ts wiring](reference_iss0018_footprint_excludes_cli_show_wiring.md) — render/show.ts is a pure formatter, so no in-footprint file can join test_results into show's CLI stdout; genuine scope_change, one-line fix specified for attempt 2.
- [ISS-0018 R4 findLineWithAll prompt-line ambiguity](reference_iss0018_r4_findlinewithall_prompt_ambiguity.md) — a SECOND, independent blocker: R4's single-token line search always selects the fixture's UserPromptSubmit line over the command's own line; scope_change alone would not turn R4 green.
- [Sandbox flaky loopback listen](reference_sandbox_flaky_loopback_listen.md) — `node:net` server.listen("127.0.0.1") and `pnpm pack` intermittently EPERM/fail in this sandbox regardless of code changes; re-run 2-3x on an unchanged tree before blaming your diff.
- [Recorded-fixture cwd collides with live sandbox repo](feedback_recorded_fixture_cwd_collides_with_live_sandbox_repo.md) — trimming a recpass-*.jsonl donor keeps its real cwd; if that scratch path still exists as a live repo here, capture silently spools into it instead of the test repo (all exits 0) — always rewrite cwd to a fake path.
- [Dynamic reason builder vs length-validator edge](feedback_dynamic_reason_builder_vs_length_validator_edge.md) — a classifier building `PREFIX+value` and a validator checking `length>PREFIX.length` silently disagree on `value=""`, wedging ingest permanently; fold empty-string to the absent branch at the source, and test with 2+ non-"clear" dynamic values plus "" in the same end-to-end matrix.
