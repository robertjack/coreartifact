// coreartifact — the local-first evidence ledger for agent-built software.
// The CLI (bins `coreartifact` + `cart`) lands with PRD-0001; this scaffold
// exists so the tsc/vitest gates run green from the first commit.
export const VERSION = "0.0.0";
